# 爬虫任务管理系统

<div align="center">

![Version](https://img.shields.io/badge/version-0.0.15-blue.svg)
![Go Version](https://img.shields.io/badge/go-1.21+-00ADD8.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**基于 uTLS 的高性能爬虫任务管理与调度平台**

[功能特性](#-功能特性) • [快速开始](#-快速开始) • [系统架构](#-系统架构) • [文档](#-文档)

</div>

---

## 📸 界面预览

![Dashboard](https://via.placeholder.com/800x450/6366f1/ffffff?text=Dashboard+Preview)

*精美的仪表板界面，实时展示任务统计、执行趋势和系统资源*

## ✨ 功能特性

### 🎯 核心功能

- **📊 可视化管理** - 精美的Web界面，直观管理所有爬虫任务
- **⏰ 智能调度** - 支持Cron定时、间隔执行、一次性任务等多种调度方式
- **📈 实时监控** - 实时查看任务状态、执行统计和系统资源使用情况
- **🚀 高性能** - 基于UTLSHotConnPool，性能提升3-6倍
- **🔧 易扩展** - 模块化设计，支持自定义任务类型和执行器

### 🛠️ 技术特性

- **🔥 热连接池** - 预建立TLS连接并复用，大幅提升性能
- **🎭 TLS指纹伪装** - 支持33种真实浏览器指纹
- **🌍 多语言支持** - 随机生成Accept-Language头，97.8%独特性
- **📡 双协议支持** - 自动检测HTTP/1.1和HTTP/2
- **🌐 双栈网络** - 完整支持IPv4和IPv6
- **🔒 安全可靠** - 死锁预防、连接健康检查、自动重试机制

## 🚀 快速开始

### 前置要求

- Go 1.21 或更高版本
- SQLite 3

### 安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/crawler-platform.git
cd crawler-platform

# 安装依赖
go mod download
```

### 运行Web管理界面

```bash
# 启动Web服务器
go run cmd/web-server/main.go

# 访问管理界面
# 浏览器打开: http://localhost:8080
```

### 使用示例

#### 创建HTTP爬虫任务

```go
package main

import (
    "crawler-platform/task"
    "time"
)

func main() {
    // 创建任务管理器
    manager := task.NewManager(&task.ManagerConfig{
        WorkerCount: 10,
        QueueSize:   1000,
    })
    
    // 创建HTTP任务
    task := &task.Task{
        Name: "爬取API数据",
        Type: task.TaskTypeHTTP,
        Config: task.TaskConfig{
            URL:    "https://api.example.com/data",
            Method: "GET",
        },
        Priority: 5,
        Schedule: &task.Schedule{
            Type:     task.ScheduleTypeCron,
            CronExpr: "0 */10 * * * *", // 每10分钟执行
        },
        Timeout: 30 * time.Second,
    }
    
    // 添加任务
    manager.AddTask(task)
    
    // 启动管理器
    manager.Start()
}
```

#### 创建Google Earth数据采集任务

```go
task := &task.Task{
    Name: "Google Earth 地形数据",
    Type: task.TaskTypeGoogleEarth,
    Config: task.TaskConfig{
        QuadtreePath: "0123",
        DataType:     "terrain",
    },
    Priority: 8,
    Schedule: &task.Schedule{
        Type:     task.ScheduleTypeInterval,
        Interval: 1 * time.Hour,
    },
}

manager.AddTask(task)
```

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                   Web 管理界面                           │
│  仪表板 | 任务管理 | 实时监控 | 执行日志 | 系统设置    │
└─────────────────────────────────────────────────────────┘
                         ↓ HTTP/WebSocket
┌─────────────────────────────────────────────────────────┐
│                    API 服务层                            │
│  任务API | 统计API | 监控API | 日志API                 │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│                  任务管理核心                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │  任务调度器 (Scheduler)                          │   │
│  │  Cron调度 | 延迟队列 | 优先级队列 | 依赖管理    │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Worker Pool (工作池)                            │   │
│  │  Worker1 | Worker2 | Worker3 | ... | WorkerN   │   │
│  └─────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  任务执行器 (Executor)                           │   │
│  │  HTTP任务 | Google Earth任务 | 自定义任务       │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│              UTLSHotConnPool (连接池)                   │
│  TLS指纹伪装 | 连接复用 | 健康检查 | IP池管理          │
└─────────────────────────────────────────────────────────┘
```

## 📦 项目结构

```
crawler-platform/
├── cmd/                      # 命令行工具
│   ├── web-server/          # Web服务器
│   └── task-manager/        # 任务管理器CLI
├── task/                     # 任务管理核心
│   ├── task.go              # 任务定义
│   ├── scheduler.go         # 任务调度器
│   ├── executor.go          # 任务执行器
│   ├── queue.go             # 任务队列
│   └── worker_pool.go       # 工作池
├── storage/                  # 存储模块
│   ├── interface.go         # 存储接口
│   ├── sqlite.go            # SQLite实现
│   └── models.go            # 数据模型
├── api/                      # API服务
│   ├── server.go            # HTTP服务器
│   ├── handlers.go          # 请求处理器
│   └── middleware.go        # 中间件
├── web/                      # Web界面
│   ├── index.html           # 主页面
│   ├── index.css            # 样式
│   └── index.js             # 前端逻辑
├── utlsclient/              # uTLS客户端核心
│   ├── utlshotconnpool.go   # 热连接池
│   ├── utlsclient.go        # HTTP客户端
│   └── utlsfingerprint.go   # TLS指纹库
├── GoogleEarth/             # Google Earth数据处理
├── docs/                     # 文档
│   ├── TASK_MANAGER_DESIGN.md  # 系统设计文档
│   └── API.md               # API文档
└── README.md                # 本文件
```

## 📊 性能指标

| 指标 | 数值 | 说明 |
|------|------|------|
| **预热速度** | 75连接/秒 | 1611个连接在21.5秒内建立完成 |
| **成功率** | 98.8% | 高可用性保证 |
| **连接复用率** | 100% | HTTP/2完美复用 |
| **性能提升** | 3-6倍 | 相比每次新建连接 |
| **指纹多样性** | 33种 | TLS指纹均匀分布 |
| **语言独特性** | 97.8% | Accept-Language组合独特性 |

## 🎯 核心模块

### 1. 任务调度器

支持多种调度方式：

- **Cron调度**: 使用Cron表达式定时执行
  ```go
  Schedule: &Schedule{
      Type:     ScheduleTypeCron,
      CronExpr: "0 */5 * * * *", // 每5分钟
  }
  ```

- **间隔调度**: 按固定间隔执行
  ```go
  Schedule: &Schedule{
      Type:     ScheduleTypeInterval,
      Interval: 1 * time.Hour, // 每小时
  }
  ```

- **一次性任务**: 立即执行一次
  ```go
  Schedule: &Schedule{
      Type: ScheduleTypeOnce,
  }
  ```

- **延迟任务**: 延迟指定时间后执行
  ```go
  Schedule: &Schedule{
      Type:  ScheduleTypeDelay,
      Delay: 10 * time.Minute,
  }
  ```

### 2. Worker Pool

- 动态调整Worker数量
- 任务窃取算法实现负载均衡
- 支持优先级队列
- 并发安全的任务分发

### 3. 任务执行器

内置三种执行器：

- **HTTP执行器**: 发送HTTP/HTTPS请求
- **Google Earth执行器**: 采集Google Earth数据
- **自定义执行器**: 支持自定义执行逻辑

### 4. 连接池

基于现有的UTLSHotConnPool：

- TLS指纹伪装（33种浏览器）
- 连接预热和复用
- 自动健康检查
- IP池管理

## 📖 文档

- [系统设计文档](docs/TASK_MANAGER_DESIGN.md) - 详细的系统架构和设计
- [实现计划](.gemini/artifacts/crawler_task_management_plan.md) - 完整的实现计划
- [API文档](docs/API.md) - RESTful API接口文档
- [架构文档](ARCHITECTURE.md) - crawler-platform架构说明

## 🔧 配置

### 任务管理器配置

```yaml
# config/task-manager.yaml
manager:
  worker_count: 10        # Worker数量
  queue_size: 1000        # 队列大小
  
storage:
  type: sqlite            # 存储类型: sqlite, mysql, postgres
  path: ./data/tasks.db   # SQLite数据库路径
  
server:
  host: 0.0.0.0
  port: 8080
  
connection_pool:
  max_connections: 1000   # 最大连接数
  idle_timeout: 300       # 空闲超时(秒)
  health_check_interval: 60  # 健康检查间隔(秒)
```

## 🧪 测试

```bash
# 运行所有测试
go test ./... -v

# 运行特定模块测试
go test ./task -v
go test ./utlsclient -v

# 运行性能测试
go test ./test -bench=. -benchmem
```

## 📈 监控指标

系统提供丰富的监控指标：

### 系统指标
- Worker使用率
- 队列长度
- 连接池使用率
- 内存使用
- CPU使用率

### 业务指标
- 任务总数
- 运行中任务数
- 成功/失败任务数
- 平均执行时间
- 成功率

### 性能指标
- API响应时间
- 任务调度延迟
- 数据库查询时间
- 连接池获取时间

## 🔒 安全特性

- JWT Token认证
- RBAC权限控制
- API密钥管理
- 敏感数据加密
- SQL注入防护
- 请求频率限制

## 🚢 部署

### Docker部署

```bash
# 构建镜像
docker build -t crawler-task-manager .

# 运行容器
docker run -d \
  -p 8080:8080 \
  -v $(pwd)/data:/app/data \
  crawler-task-manager
```

### Docker Compose

```yaml
version: '3.8'
services:
  task-manager:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - GO_ENV=production
```

## 🤝 贡献

欢迎提交Issue和Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🙏 致谢

- [refraction-networking/utls](https://github.com/refraction-networking/utls) - uTLS库
- [robfig/cron](https://github.com/robfig/cron) - Cron调度库
- [golang.org/x/net/http2](https://pkg.go.dev/golang.org/x/net/http2) - HTTP/2支持

## 📞 联系方式

- 项目主页: https://github.com/yourusername/crawler-platform
- 问题反馈: https://github.com/yourusername/crawler-platform/issues

---

<div align="center">

**⭐ 如果这个项目对你有帮助，请给它一个星标！**

Made with ❤️ by the crawler-platform team

</div>
