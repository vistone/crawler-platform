# 版本管理脚本使用说明

## 脚本列表

### 1. bump-version.sh - 版本号递增
自动将版本号的 patch 版本递增 1（如 0.0.12 → 0.0.13）

**使用方法**:
```bash
./scripts/bump-version.sh
```

**功能**:
- 自动读取当前版本号
- 递增 patch 版本号
- 更新以下文件中的版本号:
  - `cmd/utlsclient/main.go`
  - `version.go`
  - `README.md`

### 2. release.sh - 完整版本发布
自动递增版本号、提交更改、创建标签并推送到 GitHub

**使用方法**:
```bash
./scripts/release.sh
```

**交互流程**:
1. 检查是否有未提交的更改（有则询问是否继续）
2. 自动递增版本号
3. 提示输入版本更新说明
4. 创建 git commit 和 tag
5. 询问是否推送到远程仓库

**示例**:
```bash
$ ./scripts/release.sh

当前版本: v0.0.12
新版本: v0.0.13

✅ 版本号已更新到 v0.0.13

📝 请输入本次版本的更新说明 (按 Ctrl+D 结束):
───────────────────────────────────────
修复某个bug
添加新功能
优化性能
[Ctrl+D]

✅ 版本 v0.0.13 已创建

是否推送到远程仓库? (y/N) y

🎉 版本 v0.0.13 已成功发布到 GitHub!
```

## 版本号格式

版本号采用语义化版本 (Semantic Versioning): `major.minor.patch`

- **major**: 主版本号（重大更新，不兼容的API修改）
- **minor**: 次版本号（新增功能，向后兼容）
- **patch**: 修订号（bug修复，向后兼容）

当前脚本只自动递增 **patch** 版本号。如需修改 major 或 minor，请手动编辑版本号。

## 手动修改版本号

如需修改 major 或 minor 版本号，请手动编辑以下文件:

1. `cmd/utlsclient/main.go` - 修改 `const Version = "x.x.x"`
2. `version.go` - 修改 `const Version = "x.x.x"`
3. `README.md` - 修改 `**Version: vx.x.x**`

然后使用以下命令提交:
```bash
git add -A
git commit -m "chore: bump version to vx.x.x"
git tag -a vx.x.x -m "Release vx.x.x"
git push origin main && git push origin vx.x.x
```

## 快速参考

| 操作 | 命令 |
|------|------|
| 只递增版本号 | `./scripts/bump-version.sh` |
| 完整发布流程 | `./scripts/release.sh` |
| 查看当前版本 | `go run cmd/utlsclient/main.go --version` |
| 查看所有版本 | `git tag -l` |
| 查看版本详情 | `git show v0.0.12` |
